<%= link_to "Edit", edit_book_path(@book) if current_user %>
<%= link_to "Delete", book_path(@book), method: :delete, data: { confirm: "Are you sure you want to delete this book?" } if current_user %>
<%= @book.title %>
<%= @book.description %>